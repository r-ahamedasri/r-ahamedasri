<a href="https://github.com/r-ahamedasri/r-ahamedasri">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/r-ahamedasri/r-ahamedasri/main/dark_mode.svg">
    <img alt="Ahamed Asri's GitHub Profile README" src="https://raw.githubusercontent.com/r-ahamedasri/r-ahamedasri/main/light_mode.svg">
  </picture>
</a>

<!--
Auto-generated by today.py via the GitHub Action in .github/workflows/main.yml,
which runs daily and on every push to main. It pulls live repo/star/commit/
follower/LOC stats through the GitHub GraphQL API, recalculates "Uptime" from
the birthday set in today.py's __main__ block, and rewrites the id="..." text
elements inside dark_mode.svg and light_mode.svg.

The left-hand portrait is a colorized ASCII rendering of a photo, generated
once and baked directly into both SVGs as colored <tspan> text (not an
embedded image) — so it needs no external hosting and costs nothing to load.
It is static and won't change on its own; regenerate it by re-running the
image-to-ASCII conversion if you want to swap the photo.

Setup:
1. Create a fine-grained GitHub PAT with read access to:
   account: Followers, Starring, Watching
   repos:   Commit statuses, Contents, Issues, Metadata, Pull requests
2. Add it as a repo secret named ACCESS_TOKEN
   (Settings -> Secrets and variables -> Actions -> New repository secret)
3. Push this repo as r-ahamedasri/r-ahamedasri (the special repo GitHub
   renders on your profile page) and the Action will fill in real numbers
   on its next run.

Things worth double-checking / editing before you push:
- Location is set to "Colombo, Sri Lanka" as a placeholder inference from
  your university (IIT Colombo) — edit the "Location" line in both SVGs
  if that's wrong.
- GitHub's API has no "profile page views" endpoint, so that stat can't be
  wired into today.py. If you want a views counter, add a badge like:
    ![Profile views](https://komarev.com/ghpvc/?username=r-ahamedasri)
  as a separate line below the picture element.

Based on the open-source template by Andrew6rant
(https://github.com/Andrew6rant/Andrew6rant), adapted for Ahamed Asri.
-->
